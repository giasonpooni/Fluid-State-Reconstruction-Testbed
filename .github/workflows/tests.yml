name: Tests

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
    inputs:
      full_reproduction:
        description: Regenerate and verify the full simulation, calibration and sweep reports
        type: boolean
        default: false

permissions:
  contents: read

env:
  OPENBLAS_NUM_THREADS: '1'
  OMP_NUM_THREADS: '1'

jobs:
  tests:
    name: ${{ matrix.os }} / Python ${{ matrix.python }}
    runs-on: ${{ matrix.os }}
    timeout-minutes: 45
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest]
        python: ['3.12', '3.13']
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: astral-sh/setup-uv@d0cc045d04ccac9d8b7881df0226f9e82c39688e # v6.8.0
        with:
          version: '0.10.10'
      - name: Run quickstart
        run: uv run --frozen --python ${{ matrix.python }} python examples/quickstart.py
      - name: Run fluid measurement example
        run: uv run --frozen --python ${{ matrix.python }} python examples/fluid_baseline.py
      - name: Run invariant fluid example
        run: uv run --frozen --python ${{ matrix.python }} python examples/invariant_fluid.py
      - name: Run camera and gauge example
        run: uv run --frozen --python ${{ matrix.python }} python examples/camera_baseline.py --out-dir work/camera-demo
      - name: Prepare an empty recording kit
        run: uv run --frozen --python ${{ matrix.python }} python -m set_lcm.recording init work/tank-recording
      - name: Build package
        run: uv build --python ${{ matrix.python }}
      - name: Run unit gate
        run: uv run --frozen --python ${{ matrix.python }} --dev pytest -q -o addopts= -m "not slow and not extended"
      - name: Run default suite
        run: uv run --frozen --python ${{ matrix.python }} --dev pytest -q

  full-reproduction:
    if: github.event_name == 'workflow_dispatch' && inputs.full_reproduction
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4
      - uses: astral-sh/setup-uv@d0cc045d04ccac9d8b7881df0226f9e82c39688e # v6.8.0
        with:
          version: '0.10.10'
      - run: uv run --frozen --python 3.13 --dev pytest -q -rs -m slow
