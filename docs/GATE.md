# Compiled stack

This repository does not own a CUDA or Rust runtime.

Chart law, first-order covariance, and condition gates live in
Jacobian-Sensitivity-Propagation-Testbed (`docs/GATE.md`, `docs/KERNEL.md`).
Pin: `docs/JSPT_PIN.md`.

When a compiled backend exists it arrives as that gate wheel, not as CuPy
or a local T @ P @ T.T. FSRT keeps GaussianState, masks, Joseph, declaration.
