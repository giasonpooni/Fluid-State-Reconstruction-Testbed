# Accelerators

CUDA, SP1, and Rust ingest are not part of this kernel.

If a later experiment needs one, the authority is CSE
`rust-effort-gate` (`satellite-effort-v1.json`). Default is refuse.
Python dry-run in `gat.harness.effort` cannot countersign those gates.

This repo keeps hold-on-inconsistency. It does not import BIM.
